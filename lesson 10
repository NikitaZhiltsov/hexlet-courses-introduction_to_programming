// BEGIN (write your solution here)
const smallestDivisor = (num) => {
  let divisor = 2;
  if (num <= 0) {
    return NaN;
  }

  if (num  === 1) {
    return num;
  }

while (num % divisor !== 0) {
divisor += 1;
}

return divisor;
};
// END

export default smallestDivisor;
